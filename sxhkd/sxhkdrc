Print
	flameshot gui

super + b ; {h}
	{restart-sxhkd}

super + control + s ;{w}
	find $DOTFILES_PATH/wallpaper/ -type f | shuf -n 1 | xargs xwallpaper --zoom

# emacs bindings
super + e 
	emacsclient -c -a 'emacs'

