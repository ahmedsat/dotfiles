Print
	flameshot gui

super + b ; {h}
	{restart-sxhkd}

super + control + s ;{w}
	find $HOME/dotfiles/wallpaper/ -type f | shuf -n 1 | xargs xwallpaper --zoom

# emacs bindings
super + e 
	emacsclient -c -a 'emacs'

